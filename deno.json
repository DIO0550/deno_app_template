{
  "lock": true,
  "vendor": true,
  "nodeModulesDir": "none",
  "compilerOptions": {
    "strict": true,
    "lib": ["deno.window", "dom"]
  },
  "tasks": {
    "dev": "deno run --watch --allow-ffi --allow-env --allow-read --unstable-ffi main.ts",
    "start": "deno run --allow-ffi --allow-env --allow-read --unstable-ffi main.ts",
    "build": "deno compile --allow-ffi --allow-env --allow-read --unstable-ffi --output dist/app main.ts",
    "build:linux": "deno compile --target x86_64-unknown-linux-gnu --allow-ffi --allow-env --allow-read --unstable-ffi --include vendor --output dist/app-linux main.ts",
    "build:mac": "deno compile --target aarch64-apple-darwin --allow-ffi --allow-env --allow-read --unstable-ffi --include vendor --output dist/app-mac main.ts",
    "build:win": "deno compile --target x86_64-pc-windows-msvc --allow-ffi --allow-env --allow-read --unstable-ffi --include vendor --output dist/app-win.exe main.ts",
    "build:all": "deno task build:linux && deno task build:mac && deno task build:win",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test --allow-ffi --unstable-ffi",
    "cache": "deno install --frozen"
  },
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
